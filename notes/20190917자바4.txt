	<MVC패턴>

Model(logic, data처리) : db에 접속해서 작업. POJO로 작성(=그냥 자바파일)

View(presentation) : 화면에 데이터 뿌림. JSP로 작성

Controller(Servlet로 작성) : client의 요청 받기 -> Model -> View (forward로 전달)
	   ㄴ 또는 Filter(Front Controller)+POJO(그냥 controller)로 대신=필터에서 필요한 부분 pojo의 서비스메소드로 전달

	request
 	--->	"Controller"	 --->	"Model"
		(Servlet) 	          forward	(POJO)
		   |		<---	DAO      <->    DB	
client		   |			^
(html)		   |forward		VO
	response	   v			(POJO)
	<---	  "View"			
		  (JSP)			

http://localhost:8080/customer/list.cus  : 클라이언트가 할 요청
         서버:port/Context_Root/Servlet_Path

Context Root는 프로젝트의 Web Project Settings 또는 Servers의 server.xml에서 확인 가능
Context Root뒤의 /가 context 

백엔드시과목평가는 소스코드 -> 객관식 

	<저장소(scope)> .setAttribute
1.request : request가 유지될 때 까지(forward를 통해서 페이지를 이동할때 유지가 된다.)
2.session : session이 유지될 때 까지(지울때 까지!)
3.application(=context) : 서버 구동~종료까지!
아래로 갈수록 오래간다.

//req.getParameter()
  req.get또는setAttribute() : 이 req는 추가되는 부분
  